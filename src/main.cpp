#include<iostream>
using namespace std;
//name, tableno, quantity, item(menu will pop up. can be from no. too), payment method
//menu, can add (name + price)
//receipt (quantity + total)

int main(){
    
    
    return 0;
}